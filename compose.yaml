services:
  calculator:
    build:
      context: .
      target: runtime
    ports:
      - "${APP_PORT:-8080}:8080"
    environment:
      PORT: "8080"
      STATIC_DIR: /app/static
    init: true
    read_only: true
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
